"""Shared TypeScript-style types and schemas for VoxFlow.

Currently a placeholder. The Python backend already ships its own Pydantic
schemas (see apps/api/voxflow_api/schemas.py). This package is reserved for
future cross-language type sharing (e.g. via JSON Schema export + ts-codegen).
"""
